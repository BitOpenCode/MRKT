# ⚡ ECOS Marketplace - Правильная инструкция по запуску

> Запустите проект за 3-5 минут БЕЗ лишних зависимостей!

---

## 🎯 Что не так с другими инструкциями?

**❌ QUICKSTART.md и INSTALLATION.md:**
- Предлагают установить `pip install -r requirements.txt`
- Это качает **ВСЕ** зависимости включая production:
  - `gevent` (2.9 MB) - нужен только для production
  - `celery`, `redis` - для production задач
  - `psycopg2-binary` - PostgreSQL (dev использует SQLite)
  - `pytonlib`, `tonsdk`, `bitcoin` - тяжелые блокчейн библиотеки
- **Frontend .env** может иметь неправильный порт backend

**✅ Эта инструкция:**
- Устанавливает только минимум для локального запуска
- Правильные порты (backend: 5001, frontend: 3000)
- Работает быстро и без ошибок

---

## 🚀 Быстрый запуск

### Требования
- Python 3.9+
- Node.js 18+
- Git

### 1️⃣ Клонирование репозитория

```bash
git clone https://github.com/BitOpenCode/MRKT.git
cd MRKT
```

---

## 2️⃣ Backend Setup

### Шаг 1: Создать виртуальное окружение

```bash
cd backend
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
```

### Шаг 2: Установить ТОЛЬКО необходимые зависимости

**⚠️ НЕ ИСПОЛЬЗУЙТЕ `pip install -r requirements.txt` - он качает production зависимости!**

```bash
pip install Flask Flask-CORS Flask-SQLAlchemy Flask-Migrate python-dotenv requests
```

Это установит:
- ✅ Flask (веб-фреймворк)
- ✅ Flask-CORS (для связи frontend↔backend)
- ✅ Flask-SQLAlchemy (работа с БД)
- ✅ Flask-Migrate (миграции БД)
- ✅ python-dotenv (конфиг из .env)
- ✅ requests (HTTP запросы)

**Установка займет ~30 секунд вместо 5+ минут!**

### Шаг 3: Создать .env файл

```bash
cat > .env << 'EOF'
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=dev-secret-key-change-in-production
DATABASE_URL=sqlite:///marketplace.db
BITCOIN_API_URL=https://blockstream.info/api
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173
EOF
```

### Шаг 4: Создать БД и тестовые данные

```bash
python seed_data.py
```

Вы увидите:
```
📝 Test Users (password for all: password123):
------------------------------------------------------------
  Username: alice      | Level: 3 | ECOS:  1000⭐ | USDT: $25.5
  Username: bob        | Level: 4 | ECOS:  1500⭐ | USDT: $45.0
  Username: charlie    | Level: 5 | ECOS:  2000⭐ | USDT: $100.0
  ...

✅ Database seeded successfully!
Created 10 users
Created 27 contracts
Created 15 marketplace listings
```

### Шаг 5: Запустить Backend сервер

```bash
python app.py
```

Вы увидите:
```
 * Running on http://127.0.0.1:5001
 * Running on http://192.168.1.3:5001
```

✅ **Backend запущен на порту 5001!**

**Оставьте этот терминал открытым!**

---

## 3️⃣ Frontend Setup

### Шаг 1: Откройте НОВЫЙ терминал

**НЕ ЗАКРЫВАЙТЕ терминал с backend!**

### Шаг 2: Перейдите в директорию frontend

```bash
cd /путь/к/MRKT/frontend
```

### Шаг 3: Установить зависимости (если нужно)

**Проверьте есть ли папка `node_modules`:**

```bash
ls node_modules
```

**Если папки НЕТ:**
```bash
npm install
```

**Если папка ЕСТЬ - пропустите этот шаг!**

### Шаг 4: Проверить/Создать .env файл

**⚠️ ВАЖНО: Backend должен быть на порту 5001!**

```bash
cat > .env << 'EOF'
VITE_API_URL=http://localhost:5001
VITE_TON_MANIFEST_URL=http://localhost:3000/tonconnect-manifest.json
VITE_ENV=development
EOF
```

**Убедитесь что `VITE_API_URL=http://localhost:5001` (не 5000!)**

### Шаг 5: Запустить Frontend dev server

```bash
npm run dev
```

Вы увидите:
```
  VITE v5.4.21  ready in 486 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

✅ **Frontend запущен на порту 3000!**

---

## 4️⃣ Открыть приложение

### 1. Откройте браузер

Перейдите на: **http://localhost:3000**

### 2. Обновите страницу (если нужно)

Если видите "0 items available" или "Войдите чтобы увидеть контракты":

1. Нажмите **Ctrl+Shift+R** (или **Cmd+Shift+R** на Mac) - жесткий reload
2. Или очистите localStorage:
   ```javascript
   // Откройте DevTools (F12) → Console
   localStorage.clear()
   location.reload()
   ```

### 3. Войдите в систему

**Перейдите на вкладку "Profile" (иконка человека внизу)**

Нажмите **"Login"** и введите:
- **Username:** `alice`
- **Password:** `password123`

### 4. Готово! 🎉

Теперь вы должны видеть:
- ✅ **Home**: Статистику, Online Now, Active Miners
- ✅ **Market**: 15 контрактов на продажу
- ✅ **Portfolio**: 6 контрактов Alice
- ✅ **Lottery**: Билеты и розыгрыши
- ✅ **Profile**: Баланс $25.5, 1000⭐ ECOS Points

---

## 🎮 Тестовые пользователи

| Username | Password    | Level | ECOS Points | USDT    | Contracts |
|----------|------------|-------|-------------|---------|-----------|
| alice    | password123| 3     | 1000⭐      | $25.5   | 6         |
| bob      | password123| 4     | 1500⭐      | $45.0   | 0         |
| charlie  | password123| 5     | 2000⭐      | $100.0  | 4         |
| david    | password123| 1     | 1000⭐      | $10.0   | 0         |
| emma     | password123| 2     | 1200⭐      | $15.75  | 0         |

**Рекомендуется тестировать с `alice` или `charlie` - у них есть контракты!**

---

## 🐛 Troubleshooting

### Проблема 1: "0 items available" на маркетплейсе

**Причина:** Frontend не может подключиться к backend

**Решение:**

1. Проверьте что backend запущен и работает:
   ```bash
   curl http://localhost:5001/api/marketplace/listings
   ```
   Должен вернуть JSON с контрактами

2. Проверьте `.env` во frontend:
   ```bash
   cat frontend/.env
   ```
   Должно быть: `VITE_API_URL=http://localhost:5001` (не 5000!)

3. Если порт неправильный - исправьте и перезапустите frontend:
   ```bash
   # Ctrl+C в терминале frontend
   npm run dev
   ```

4. Обновите страницу в браузере (Ctrl+Shift+R)

---

### Проблема 2: Backend не запускается (ModuleNotFoundError)

**Ошибка:** `ModuleNotFoundError: No module named 'flask'`

**Решение:**

1. Убедитесь что venv активирован:
   ```bash
   source venv/bin/activate
   # Должно появиться (venv) в начале строки
   ```

2. Переустановите зависимости:
   ```bash
   pip install Flask Flask-CORS Flask-SQLAlchemy Flask-Migrate python-dotenv requests
   ```

---

### Проблема 3: Порт занят

**Ошибка:** `Address already in use` или `EADDRINUSE`

**Backend (порт 5001):**
```bash
lsof -ti:5001 | xargs kill -9
```

**Frontend (порт 3000):**
```bash
lsof -ti:3000 | xargs kill -9
```

Затем перезапустите серверы.

---

### Проблема 4: БД пустая / Login не работает

**Решение:**

```bash
cd backend
source venv/bin/activate
rm -f instance/marketplace.db  # Удалить старую БД
python seed_data.py             # Создать новую
```

Обновите страницу и заново войдите.

---

### Проблема 5: Frontend не загружается после установки

**Решение:**

```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

---

## 📊 Что работает

### ✅ Реализованные функции:

1. **Marketplace** - покупка/продажа контрактов
2. **Portfolio** - управление контрактами
3. **Mining** - запуск майнинга (лок на 7 дней)
4. **Lottery** - покупка билетов, розыгрыши
5. **Wallet** - TON Connect, баланс USDT/BTC
6. **ECOS Points** - скидки до 30%
7. **Referral System** - до 5% с рефералов
8. **Темы** - темная/светлая
9. **i18n** - 10 языков
10. **Authentication** - session-based auth

---

## 🔧 Что НЕ работает (требует доп. зависимостей)

### ❌ Функции требующие production зависимости:

1. **Лотерея с Bitcoin hash** - требует `bitcoin` library
2. **TON NFT импорт/экспорт** - требует `pytonlib`, `tonsdk`
3. **Celery задачи** - требует `celery`, `redis`
4. **PostgreSQL** - требует `psycopg2-binary`

**Для тестирования базовой функциональности это не критично!**

Если нужно это включить:
```bash
pip install -r requirements.txt  # Установит всё, включая production!!!!!!!!ВАЖНО
```

---

## 📝 Итого: Что мы установили

### Backend (минимальный набор)
```bash
Flask                  # Веб-фреймворк
Flask-CORS             # CORS поддержка
Flask-SQLAlchemy       # ORM для БД
Flask-Migrate          # Миграции БД
python-dotenv          # Конфиг из .env
requests               # HTTP запросы
```

**+ их зависимости (SQLAlchemy, Werkzeug, Jinja2, etc.)**

**Размер установки: ~10 MB вместо 50+ MB**

### Frontend
```bash
npm install  # Если не было node_modules
```

**Всё! Проект работает! 🚀**

---

## 🔗 Полезные ссылки

- 📖 [README.md](./README.md) - Полное описание проекта
- 📦 [INSTALLATION.md](./INSTALLATION.md) - Подробная установка (с production зависимостями)
- 📊 [FEATURES_STATUS.md](./FEATURES_STATUS.md) - Статус всех функций
- 📝 [TEST_USERS_CREDENTIALS.md](./TEST_USERS_CREDENTIALS.md) - Список тестовых пользователей
- 🏗️ [ARCHITECTURE.md](./ARCHITECTURE.md) - Архитектура проекта

