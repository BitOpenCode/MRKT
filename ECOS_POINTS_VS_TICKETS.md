# ECOS Points vs Lottery Tickets - Важное различие

## 🎯 Две разные сущности

### ⭐ ECOS Points (Баллы лояльности)

**Что это:**
- Внутренняя валюта лояльности
- Начисляются за активность (покупки, рефералов)
- Используются для скидок (до 30%)

**Как получить:**
- За покупки на маркетплейсе (кэшбэк)
- За приглашенных друзей (реферальная программа)
- За достижения в системе уровней

**Как использовать:**
- Скидка до 30% при покупке лотерейных билетов (Level 5)
- Скидка до 30% при покупке на маркетплейсе (Level 5)
- **НЕ ЯВЛЯЮТСЯ** лотерейными билетами

**Где хранятся:**
```typescript
user.ecosPoints: number  // Баланс ECOS Points
```

**Пример использования:**
```
Покупка 10 билетов:
- Обычная цена: $5.00 USDT
- С ECOS Points (30%): $3.50 USDT + 150⭐
```

---

### 🎫 Lottery Tickets (Лотерейные билеты)

**Что это:**
- Билеты для участия в лотерее
- Каждый билет имеет уникальный номер
- Участвуют в розыгрыше призов

**Как получить:**
- Купить за USDT ($0.50 за билет)
- Можно использовать ECOS Points для скидки

**Как использовать:**
- Автоматически участвуют в розыгрышах
- Победитель определяется по наименьшему хэшу
- После розыгрыша билет сгорает

**Где хранятся:**
```typescript
// В базе данных
LotteryTicket {
  id: string
  user_id: string
  ticket_number: number
  status: 'pending' | 'won' | 'lost'
  draw_id?: string
}
```

**Пример:**
```
User alice:
- ECOS Points: 5,000⭐ (для скидок)
- Lottery Tickets: 3 билета (участвуют в розыгрыше)
```

---

## 🔀 Взаимодействие

### ✅ ПРАВИЛЬНО:
```typescript
// 1. Пользователь покупает билеты за USDT
const ticketPrice = 0.50 // USDT
const ticketCount = 10
const totalPrice = ticketPrice * ticketCount // $5.00

// 2. ОПЦИОНАЛЬНО: Использовать ECOS Points для скидки
if (useEcosPoints && user.level === 5) {
  const discount = 0.30 // 30%
  const finalPrice = totalPrice * (1 - discount) // $3.50
  const pointsUsed = totalPrice * discount * 100 // 150⭐
  
  user.usdtBalance -= finalPrice
  user.ecosPoints -= pointsUsed
}

// 3. Создать лотерейные билеты
for (let i = 0; i < ticketCount; i++) {
  createLotteryTicket(user.id)
}
```

---

## 📊 Сравнение

| Параметр | ECOS Points ⭐ | Lottery Tickets 🎫 |
|----------|----------------|-------------------|
| **Тип** | Баллы лояльности | Лотерейные билеты |
| **Получение** | Кэшбэк, рефералы | Покупка за USDT |
| **Использование** | Скидки (до 30%) | Участие в розыгрышах |
| **Хранение** | `user.ecosPoints` | Таблица `lottery_tickets` |
| **Сгорают?** | Нет | Да (после розыгрыша) |
| **Можно обменять?** | Нет | Нет |

---

## 🎮 Примеры из интерфейса

### Profile Page:
```typescript
// ECOS Points (баллы лояльности)
<div className="stat">
  <Star className="w-5 h-5 text-warning" />
  <p className="text-2xl font-bold">{user.ecosPoints.toLocaleString()}⭐</p>
  <p className="text-xs">ECOS Points</p>
</div>
```

### Lottery Page:
```typescript
// Lottery Tickets (лотерейные билеты)
<div className="stat">
  <Ticket className="w-5 h-5 text-warning" />
  <p className="text-2xl font-bold">{userTickets.length}</p>
  <p className="text-xs">Active Tickets</p>
</div>
```

---

## 🚀 Best Practices

1. **Всегда различайте:**
   - "Buy tickets with USDT" (покупка билетов)
   - "Use ECOS Points for discount" (использование баллов для скидки)

2. **Четкие названия:**
   - `user.ecosPoints` - баллы
   - `user.tickets` - билеты
   - НЕ путать!

3. **Интерфейс:**
   - ECOS Points: звезда ⭐
   - Lottery Tickets: билет 🎫

4. **Документация:**
   - Указывайте в комментариях что именно используется
   - Не смешивайте логику баллов и билетов

---

## ✅ Итог

**ECOS Points** и **Lottery Tickets** - это **две разные вещи**:
- ECOS Points = валюта лояльности (для скидок)
- Lottery Tickets = билеты для розыгрышей (участие в лотерее)

**Связь:**
- ECOS Points могут дать скидку при покупке билетов
- Билеты покупаются за USDT (с возможной скидкой от ECOS Points)
