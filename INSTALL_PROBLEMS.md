# ⚠️ Проблемы со старыми инструкциями по установке

## 🐛 Что было не так

### Проблема 1: requirements.txt включает production зависимости

**Файл:** `backend/requirements.txt`

**Что внутри:**
```txt
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
python-dotenv==1.0.0
requests==2.31.0
psycopg2-binary==2.9.9    # ❌ PostgreSQL - не нужен для dev
redis==5.0.1              # ❌ Redis - не нужен для dev
celery==5.3.4             # ❌ Celery - не нужен для dev
pytonlib==0.0.48          # ❌ TON library - тяжелая, не нужна для базовой работы
tonsdk==1.0.13            # ❌ TON SDK - тяжелая, не нужна для базовой работы
bitcoin==1.1.42           # ❌ Bitcoin library - не нужна для базовой работы

# For production
gunicorn==21.2.0          # ❌ Production сервер - не нужен для dev
gevent==23.9.1            # ❌ 2.9 MB! Async workers - не нужен для dev
```

**Проблема:**
- Установка занимает 5+ минут
- Качает 50+ MB зависимостей
- `gevent` требует компиляцию C-расширений
- Часто вылетают ошибки при установке (особенно на Windows)
- Блокчейн библиотеки тяжелые и не нужны для базовой работы

**Решение:**
Используйте `requirements-dev.txt` для локальной разработки:
```bash
pip install -r requirements-dev.txt  # Только необходимое, ~30 секунд
```

---

### Проблема 2: Неправильный порт в .env frontend

**Файлы:**
- `QUICKSTART.md`
- `setup.sh`

**Что было:**
```env
VITE_API_URL=http://localhost:5000
```

**Проблема:**
- Backend запускается на порту **5001**
- Frontend пытается подключиться к порту **5000**
- Результат: "0 items available", данные не загружаются

**Решение:**
```env
VITE_API_URL=http://localhost:5001  # Правильный порт!
```

---

### Проблема 3: setup.sh не учитывает уже установленные зависимости

**Файл:** `setup.sh`

**Что делает:**
1. Всегда запускает `npm install` во frontend (даже если node_modules есть)
2. Всегда запускает `pip install -r requirements.txt` (все зависимости)
3. Не проверяет существующие .env файлы

**Проблема:**
- Медленно (переустанавливает всё каждый раз)
- Может затереть правильные настройки в .env

**Решение:**
Используйте ручную установку по инструкции [START_CORRECT.md](./START_CORRECT.md)

---

## ✅ Правильный подход

### Что нужно для локальной разработки:

**Backend (минимум):**
```bash
Flask                  # Веб-фреймворк
Flask-CORS             # CORS поддержка
Flask-SQLAlchemy       # ORM для БД
Flask-Migrate          # Миграции БД
python-dotenv          # Конфиг из .env
requests               # HTTP запросы
```

**Frontend:**
```bash
npm install  # Один раз, если нет node_modules
```

**Конфигурация:**
- Backend: порт 5001
- Frontend: порт 3000
- Frontend .env: `VITE_API_URL=http://localhost:5001`

---

## 📚 Правильные инструкции

1. **[START_CORRECT.md](./START_CORRECT.md)** - ✅ Правильная полная инструкция
2. **[START.md](./START.md)** - ✅ Краткая шпаргалка
3. **[requirements-dev.txt](./backend/requirements-dev.txt)** - ✅ Минимальные зависимости

---

## 🚫 НЕ используйте (устарело/неправильно):

1. ❌ `QUICKSTART.md` - неправильный порт, требует полную установку
2. ❌ `INSTALLATION.md` - слишком подробно, требует полную установку
3. ❌ `setup.sh` - автоматический скрипт с проблемами
4. ❌ `pip install -r requirements.txt` - устанавливает production зависимости

---

## 💡 Когда использовать полную установку

**Production деплой:**
```bash
pip install -r requirements.txt  # Все зависимости
```

**Если нужны фичи:**
- Bitcoin лотерея с хэшами
- TON NFT импорт/экспорт
- Celery фоновые задачи
- PostgreSQL БД

**Для базового тестирования - НЕ НУЖНО!**

---

## 🔧 Как исправить если уже установили неправильно

### 1. Удалите venv и переустановите:

```bash
cd backend
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements-dev.txt  # Только минимум
```

### 2. Проверьте .env во frontend:

```bash
cat frontend/.env
# Должно быть: VITE_API_URL=http://localhost:5001
```

Если неправильно:
```bash
cat > frontend/.env << 'EOF'
VITE_API_URL=http://localhost:5001
VITE_TON_MANIFEST_URL=http://localhost:3000/tonconnect-manifest.json
VITE_ENV=development
EOF
```

### 3. Перезапустите frontend:

```bash
cd frontend
npm run dev
```

### 4. Обновите страницу в браузере:

```javascript
// DevTools Console (F12)
localStorage.clear()
location.reload()
```

---

**Готово! Теперь всё должно работать! 🎉**
